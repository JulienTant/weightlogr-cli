# Project Rules

- All claims about the codebase must be grounded in source code or documentation. Never guess or assume — read the code first.
- Always use `task` commands (e.g. `task test`, `task lint`, `task e2e`, `task check`) instead of running tools directly. See `Taskfile.yml` for available tasks.
